[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NEXT_PUBLIC_BACKEND_URL = "https://your-backend-url.com/api" # User needs to replace this when deploying backend
